# Secrets & runtime config (copy examples and fill in values)
backend/.env
backend/dashboard_config.json
backend/dashboard.db
backend/dashboard.db-shm
backend/dashboard.db-wal

# Dev-only notes — kept locally, not published. These contain internal LAN IPs,
# the production Pi address, and the old default SSH password, so they must not
# ship in the public repo. Edit them freely; git just won't track them.
/CLAUDE.md
/DASHBOARD_PROJECT_CONTEXT.md
/PROJECT_SUMMARY.md
/Dashboard_Summary.docx
/Roadmap.docx

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
!frontend/dist/
.pytest_cache/

# Node
frontend/node_modules/
frontend/.vite/
# frontend/dist/ is intentionally tracked — Pi images clone from GitHub; no npm on device.

# Pi image builder — large binary artifacts
pi/.cache/
pi/output/

# OS
.DS_Store
Thumbs.db
